package array;

public class ArrayDocs {
	/*
	 *    ============================ Array =================================================
	 *    # use to store and processing static(fixed size) multiple data of same data type.
	 *    # collection/group of multiple data of same data type.
	 *    # collection of homogeneous data.
	 *    # data stored in the basis of indices(i.e. index starts from 0)
	 *    # random access
	 *    # contains duplicate data
	 *    # contiguous memory allocation.
	 *    
	 *    syntax :
	 *    
	 *      data_type  array_name[] = new data_type[size];
	 *     
	 */

}
